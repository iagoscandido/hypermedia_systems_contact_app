[project]
name = "hypermedia-systems-contact-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["flask>=3.1.3", "pydantic>=2.13.5"]

[build-system]
requires = ["uv_build>=0.12.13,<0.13"]
build-backend = "uv_build"
